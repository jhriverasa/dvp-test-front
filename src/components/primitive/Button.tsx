import { styled } from "@mui/material/styles";
import MUIButton from "@mui/material/Button"; // We use alias to avoid problems when naming our component

const Button = styled(MUIButton)({});

export default Button;
